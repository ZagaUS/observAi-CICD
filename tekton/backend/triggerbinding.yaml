apiVersion: triggers.tekton.dev/v1alpha1
kind: TriggerBinding
metadata:
  name: github-binding
  namespace: observai-main
spec:
  params:
    - name: url
      value: $(body.repository.clone_url)





